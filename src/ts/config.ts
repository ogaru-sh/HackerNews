import { Config } from "./interface";

const config: Config = {
	apiInfo: {
		baseUrl: "https://hacker-news.firebaseio.com/v0",
		query: "print=pretty",
    },
    viewLimit: 10
};

export default config;
